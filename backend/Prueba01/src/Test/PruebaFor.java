package Test;

import javax.swing.JOptionPane;

public class PruebaFor {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		/*
		 * int arroba = 0; boolean punto = false;
		 * 
		 * String cadena = JOptionPane.showInputDialog("Introduce tu email");
		 * 
		 * for (int i = 0; i < cadena.length(); i++) {
		 * 
		 * if (cadena.charAt(i) == '@') { arroba++; }
		 * 
		 * if (cadena.charAt(i) == '.') { punto = true; } }
		 * 
		 * if (arroba == 1) { JOptionPane.showMessageDialog(null, "Email correcto"); }
		 * else { JOptionPane.showMessageDialog(null, "Email incorrecto"); } }
		 */

		/*
		 * int resultado = 1;
		 * 
		 * int numero = Integer.parseInt(JOptionPane.
		 * showInputDialog("Introduce un número para calcular el factorial"));
		 * 
		 * for (int i = numero; i > 0; i--) { resultado = resultado * i; }
		 * 
		 * JOptionPane.showMessageDialog(null, "El factorial de " + numero + " es " +
		 * resultado);
		 */
	}

}
